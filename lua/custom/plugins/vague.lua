return {
  'vague2k/vague.nvim',
  config = function()
    -- NOTE: you do not need to call setup if you don't want to.
    require('vague').setup {
      -- style = {
      --   string = 'none',
      -- },
      -- colors = {
      --   bg = '#222224',
      --   fg = '#D7D7D7',
      -- },
    }
  end,
}
